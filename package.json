{
    "name": "skill-swap-root",
    "version": "1.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "cd client && npm install && npm run build && cd .. && cp -r client/dist dist"
    },
    "dependencies": {
        "bcryptjs": "^3.0.3",
        "cors": "^2.8.5",
        "dotenv": "^17.2.3",
        "express": "^5.2.1",
        "jsonwebtoken": "^9.0.3",
        "mongoose": "^9.1.2"
    }
}